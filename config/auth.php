<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Authentication Defaults
    |--------------------------------------------------------------------------
    |
    | هنا نحدد الـ guard الافتراضي ونظام إعادة تعيين كلمات المرور.
    | بما إنو مشروعك يعتمد على تسجيل دخول المدراء فقط،
    | لازم نخلي الـ guard الافتراضي هو "admin" بدل "web".
    |
    */

    'defaults' => [
        'guard' => 'admin',
        'passwords' => 'admins',
    ],

    /*
    |--------------------------------------------------------------------------
    | Authentication Guards
    |--------------------------------------------------------------------------
    |
    | هنا نحدد الـ guards المتاحة. كل guard يحدد طريقة تسجيل الدخول
    | والـ provider اللي بيجيب المستخدمين منه.
    |
    */

    'guards' => [

        // Guard الويب الافتراضي (لوحة المستخدمين)
        'web' => [
            'driver' => 'session',
            'provider' => 'users',
        ],

        // Guard المدراء (لوحة التحكم)
        'admin' => [
            'driver' => 'session',
            'provider' => 'admins',
        ],

        // Guard المستخدمين عبر API باستخدام Passport
        'api' => [
            'driver' => 'passport',
            'provider' => 'users',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | User Providers
    |--------------------------------------------------------------------------
    |
    | هنا نحدد من وين Laravel بيجيب بيانات المستخدمين.
    | كل provider مرتبط بجدول وموديل معيّن.
    |
    */

    'providers' => [

        // Provider المستخدمين (User Model)
        'users' => [
            'driver' => 'eloquent',
            'model' => App\Models\User::class,
        ],

        // Provider المدراء (Admin Model)
        'admins' => [
            'driver' => 'eloquent',
            'model' => App\Models\Admin::class,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Resetting Passwords
    |--------------------------------------------------------------------------
    |
    | هنا نحدد إعدادات إعادة تعيين كلمة المرور لكل provider.
    | لازم نضيف قسم خاص بالمدراء.
    |
    */

    'passwords' => [
        'users' => [
            'provider' => 'users',
            'table' => 'password_reset_tokens',
            'expire' => 60,
            'throttle' => 60,
        ],

        'admins' => [
            'provider' => 'admins',
            'table' => 'password_reset_tokens',
            'expire' => 60,
            'throttle' => 60,
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | Password Confirmation Timeout
    |--------------------------------------------------------------------------
    |
    | الوقت قبل ما يطلب Laravel تأكيد كلمة المرور من جديد.
    |
    */

    'password_timeout' => 10800,

];
